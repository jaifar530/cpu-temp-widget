# CPU Temperature Widget - Dependencies

# Core UI framework
PyQt6>=6.6.0

# Windows temperature reading via WMI
WMI>=1.5.1

# Optional: For direct hardware monitoring (requires admin privileges)
# pythonnet>=3.0.3  # Uncomment if using LibreHardwareMonitor DLL

# Build tools
pyinstaller>=6.3.0
