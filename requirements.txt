# CPU Temperature Widget - Dependencies
# NOTE: Requires Python 3.12 for stable pythonnet support

# Core UI framework
PyQt6>=6.6.0

# Windows temperature reading via WMI
WMI>=1.5.1

# .NET interop for LibreHardwareMonitorLib DLL
pythonnet>=3.0.3

# Build tools
pyinstaller>=6.3.0
